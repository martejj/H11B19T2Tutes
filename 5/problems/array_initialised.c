#include <stdio.h>

int array_initialised(int length, int nums[]);

int main(void) {
    int array[10];
    array_initialised(10, array);
}

int array_initialised(int length, int nums[]) {
    
    return length;
    
}
